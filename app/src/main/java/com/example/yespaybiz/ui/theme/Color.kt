package com.example.yespaybiz.ui.theme

import androidx.compose.ui.graphics.Color

// Brand — Figma "Primary/Primary CTA"
val PrimaryBlue = Color(0xFF234E91)
val PrimaryCTA = Color(0xFF2A7DC0)
val SecondaryBlue = Color(0xFFE8F0FE)
val BackgroundLight = Color(0xFFF5F7FB)

// Text — Figma tokens
val TextDark = Color(0xFF222222)
val TextGray = Color(0xFF677F9D)
val TextMuted = Color(0xFF8E9BB1)

// Navigation Bar
val NavBarSelectedIcon = Color(0xFF1C85DB)
val NavBarSelectedText = Color(0xFF2177BC)

// Semantic
val SuccessGreen = Color(0xFF4CAF50)
val ErrorRed = Color(0xFFDA5555)
val CardBackground = Color(0xFFFFFFFF)

// Transaction filters
val FilterPurple = Color(0xFF7066E0)
val FilterTeal = Color(0xFF48C4B5)

// Settlement — Figma "Pro Card BG" gradient
val PendingCyanLight = Color(0xFFC7E7F9)
val PendingCyanDark = Color(0xFFA2E6E1)

// Settlement — Amount tag gradient ("Trial/Blue2 GRD")
val AmountTagStart = Color(0xFF40A2DA)
val AmountTagEnd = Color(0xFF37BEC7)

// Settlement — Alert banner gradient ("Trial/BG4")
val AlertBannerStart = Color(0xFFFCD5D5)
val AlertBannerEnd = Color(0xFFEEE2C2)

// Settlement — Alert title gradient
val AlertTitleStart = Color(0xFF005192)
val AlertTitleEnd = Color(0xFF0072B1)

// Settlement — Tags
val SettledTagText = Color(0xFF798CC2)
val SettledTagBorder = Color(0x33798CC2) // rgba(121,140,194,0.2)

// Settlement — Item card
val ItemCardBg = Color(0xFFF6F7FC)
val StrokeLight = Color(0xFFE3E9EE)

// QR / UPI
val UpiOrange = Color(0xFFEA580C)